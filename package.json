{
  "name": "lampda-bookmarks-plugins",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rollup -c",
    "serve": "http-server dist -p 8080 -c-1",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "jsdom": "^25.0.0",
    "rollup": "^4.62.2",
    "vitest": "^2.0.0"
  }
}
